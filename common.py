"""
Shared config / chunking / OpenRouter helpers.

Phase 1 placeholder — full port from the private Masadir stack in Phase 2.
"""

__all__: list[str] = []


def not_implemented(feature: str = "RAG core") -> None:
    raise NotImplementedError(
        f"{feature} arrives in Phase 2. "
        "Schema + sample fixture are available now; see README.md."
    )
